# 🚀 Инструкция по деплою приложения LAppka

## Подготовка к деплою

### 1. Проверка файлов

Убедитесь, что в проекте есть:
- ✅ `app_new.py` - главный файл приложения
- ✅ `requirements.txt` - зависимости
- ✅ `.streamlit/config.toml` - конфигурация Streamlit
- ✅ `.gitignore` - правильно настроен
- ✅ `Лого.jpg` - логотип приложения (в корне проекта)

### 2. Деплой на Streamlit Cloud

#### Шаг 1: Подготовка репозитория

1. Убедитесь, что проект загружен в GitHub:
   ```bash
   git add .
   git commit -m "Подготовка к деплою"
   git push origin main
   ```

#### Шаг 2: Деплой через Streamlit Cloud

1. Откройте https://share.streamlit.io
2. Войдите через GitHub
3. Нажмите **"New app"**
4. Заполните форму:
   - **Repository**: выберите ваш репозиторий `LAppka`
   - **Branch**: `main` (или ваша основная ветка)
   - **Main file path**: `app_new.py` ⚠️ **ВАЖНО: укажите app_new.py**
   - **App URL**: выберите уникальный URL (например, `lappka`)

5. Нажмите **"Deploy!"**

#### Шаг 3: Настройка после деплоя

После успешного деплоя:
- Приложение будет доступно по адресу: `https://lappka.streamlit.app`
- Streamlit автоматически установит зависимости из `requirements.txt`
- Если возникнут ошибки, проверьте логи в панели управления

### 3. Альтернативные платформы

#### Heroku

1. Создайте `Procfile`:
   ```
   web: streamlit run app_new.py --server.port=$PORT --server.address=0.0.0.0
   ```

2. Создайте `setup.sh`:
   ```bash
   mkdir -p ~/.streamlit/
   echo "[server]
   headless = true
   port = $PORT
   enableCORS = false
   " > ~/.streamlit/config.toml
   ```

3. Деплой через Heroku CLI:
   ```bash
   heroku create lappka-app
   git push heroku main
   ```

#### Docker

Создайте `Dockerfile`:
```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8501

CMD ["streamlit", "run", "app_new.py", "--server.port=8501", "--server.address=0.0.0.0"]
```

Запуск:
```bash
docker build -t lappka .
docker run -p 8501:8501 lappka
```

### 4. Проверка после деплоя

После деплоя проверьте:
- ✅ Приложение загружается без ошибок
- ✅ Логотип отображается корректно
- ✅ Все модули работают
- ✅ Загрузка файлов функционирует
- ✅ Графики отображаются правильно

### 5. Возможные проблемы

#### Проблема: Логотип не отображается
**Решение**: Убедитесь, что файл `Лого.jpg` находится в корне проекта и закоммичен в git

#### Проблема: Ошибка импорта модулей
**Решение**: Проверьте, что все файлы модулей находятся в правильных папках и закоммичены

#### Проблема: Зависимости не устанавливаются
**Решение**: Проверьте `requirements.txt` на наличие всех необходимых пакетов

#### Проблема: Приложение не запускается
**Решение**: 
- Проверьте логи в панели управления Streamlit Cloud
- Убедитесь, что главный файл указан как `app_new.py`
- Проверьте синтаксис Python кода

### 6. Обновление приложения

После внесения изменений:
```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

Streamlit Cloud автоматически перезапустит приложение с новыми изменениями.

### 7. Переменные окружения (если понадобятся)

Если в будущем понадобятся секретные ключи:
1. В Streamlit Cloud: Settings → Secrets
2. Добавьте переменные в формате TOML:
   ```toml
   [secrets]
   API_KEY = "your_key_here"
   ```

### 8. Мониторинг

- **Логи**: доступны в панели управления Streamlit Cloud
- **Использование**: отслеживайте в разделе "Usage"
- **Ошибки**: проверяйте логи при возникновении проблем

---

**Готово к деплою!** 🎉

Если возникнут вопросы, проверьте документацию Streamlit Cloud: https://docs.streamlit.io/streamlit-community-cloud
