# 👨‍💻 Руководство для разработчиков

## Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Запуск приложения
```bash
streamlit run app_new.py
```

### 3. Структура для начинающих

Если вы начинающий программист, вот что нужно знать:

#### Основные папки:
- **config/**: Здесь хранятся настройки приложения
- **modules/**: Здесь находится вся логика приложения
- **utils/**: Вспомогательные функции
- **static/**: CSS стили и другие ресурсы

## Как работает приложение

### 1. Точка входа
Файл `app_new.py` - это главный файл приложения. Он:
- Настраивает страницу Streamlit
- Загружает CSS стили
- Создает вкладки для модулей
- Вызывает функции рендеринга каждого модуля

### 2. Модули
Каждый модуль имеет:
- Папку с названием модуля в `modules/`
- Файл `module.py` с функцией `render()`
- Файл `__init__.py` (пустой, но обязательный)

### 3. Базовые модули
В `modules/base/` находятся общие функции:
- **data_loader.py**: Загружает файлы
- **data_parser.py**: Обрабатывает данные
- **visualizer.py**: Отображает данные

## Как добавить новую функцию

### Шаг 1: Создайте модуль
```python
# modules/my_new_module/__init__.py
# (пустой файл)

# modules/my_new_module/module.py
import streamlit as st

def render():
    st.markdown("### Мой новый модуль")
    st.write("Здесь будет функциональность")
```

### Шаг 2: Добавьте в настройки
```python
# config/settings.py
MODULES = {
    # ... существующие модули
    "my_new_module": {
        "name": "🆕 Мой модуль",
        "description": "Описание модуля"
    }
}
```

### Шаг 3: Добавьте в главный файл
```python
# app_new.py
from modules.my_new_module.module import render as render_my_new_module

# В функции main():
tab_functions = {
    0: render_question_analysis,
    1: render_statistics,
    2: render_discrimination,
    3: render_reports,
    4: render_search,
    5: render_settings,
    6: render_my_new_module  # Добавьте сюда
}
```

## Работа с данными

### Загрузка файлов
```python
from modules.base.data_loader import load_data

# Загрузка файла
data = load_data(uploaded_file)
```

### Парсинг данных
```python
from modules.base.data_parser import parse_question_data, parse_answers_data

# Парсинг вопросов
questions = parse_question_data(data)

# Парсинг ответов
answers = parse_answers_data(data)
```

### Отображение данных
```python
from modules.base.visualizer import display_question_analysis

# Отображение анализа
display_question_analysis(questions, answers)
```

## Стилизация

### CSS классы
Используйте классы из `static/css/styles.css`:
- `.main-header`: Заголовок приложения
- `.module-card`: Карточка модуля
- `.question-block`: Блок вопроса
- `.metric-badge`: Значок метрики

### Пример использования
```python
st.markdown("""
<div class="question-block easy">
    <h4>Вопрос 1.1: Название вопроса</h4>
</div>
""", unsafe_allow_html=True)
```

## Полезные функции

### Безопасное преобразование типов
```python
from utils.helpers import safe_float, safe_int

value = safe_float("123.45", default=0.0)  # 123.45
value = safe_float("invalid", default=0.0)  # 0.0
```

### Определение классов для метрик
```python
from utils.helpers import get_metric_class, get_difficulty_color

# Класс для метрики
class_name = get_metric_class("25", "discrimination")  # "metric-warning"

# Цвет сложности
color = get_difficulty_color("75")  # "easy"
```

## Отладка

### Просмотр данных
```python
# В любом модуле
with st.expander("Отладка"):
    st.write({"questions": len(questions), "data": data})
```

### Проверка типов файлов
```python
# Проверка поддерживаемых типов
from config.settings import SUPPORTED_FILE_TYPES
st.write(f"Поддерживаемые типы: {SUPPORTED_FILE_TYPES}")
```

## Лучшие практики

### 1. Структура модуля
```python
def render():
    """Основная функция рендеринга модуля"""
    # Заголовок
    st.markdown("### Название модуля")
    
    # Описание
    st.markdown("**Описание:** Что делает модуль")
    
    # Основная логика
    # ...
```

### 2. Обработка ошибок
```python
try:
    # Код, который может вызвать ошибку
    result = some_function()
except Exception as e:
    st.error(f"Ошибка: {e}")
```

### 3. Комментарии
```python
# Хороший комментарий объясняет "зачем", а не "что"
def calculate_difficulty(question):
    """Расчет сложности вопроса на основе процента правильных ответов"""
    # Плохой комментарий: "Получаем значение"
    # Хороший комментарий: "Нормализуем значение для корректного расчета"
    normalized_value = float(question['correct_percentage'])
    return normalized_value
```

## Расширение функциональности

### Добавление новых типов файлов
1. Добавьте тип в `config/settings.py`
2. Обновите функцию `load_data()` в `modules/base/data_loader.py`
3. Добавьте соответствующий парсер в `modules/base/data_parser.py`

### Добавление новых метрик
1. Добавьте константы в `utils/constants.py`
2. Обновите функции в `utils/helpers.py`
3. Добавьте отображение в `modules/base/visualizer.py`

## Полезные ресурсы

- [Документация Streamlit](https://docs.streamlit.io/)
- [Pandas документация](https://pandas.pydata.org/docs/)
- [BeautifulSoup документация](https://www.crummy.com/software/BeautifulSoup/bs4/doc/)

## Вопросы и поддержка

Если у вас есть вопросы по архитектуре или разработке:
1. Изучите существующий код в модулях
2. Посмотрите примеры в `modules/question_analysis/`
3. Обратитесь к документации в папке `docs/`
