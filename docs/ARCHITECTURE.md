# 🏗️ Архитектура приложения

## Обзор

Приложение построено по модульной архитектуре, что делает его легко расширяемым и понятным для начинающих программистов.

## Структура проекта

```
LAppka/
├── app.py                 # Старый главный файл (для справки)
├── app_new.py            # Новый главный файл с модульной архитектурой
├── config/               # Конфигурация
│   ├── __init__.py
│   └── settings.py       # Настройки приложения
├── modules/              # Модули приложения
│   ├── __init__.py
│   ├── base/             # Базовые классы и утилиты
│   │   ├── __init__.py
│   │   ├── data_loader.py    # Загрузка данных
│   │   ├── data_parser.py    # Парсинг данных
│   │   └── visualizer.py     # Визуализация
│   ├── question_analysis/   # Модуль 1: Анализ вопросов
│   ├── statistics/          # Модуль 2: Статистика
│   ├── discrimination/      # Модуль 3: Дискриминация
│   ├── reports/             # Модуль 4: Отчеты
│   ├── search/              # Модуль 5: Поиск
│   └── settings/            # Модуль 6: Настройки
├── utils/               # Утилиты
│   ├── __init__.py
│   ├── helpers.py           # Вспомогательные функции
│   └── constants.py         # Константы
├── static/              # Статические файлы
│   ├── css/
│   │   └── styles.css       # CSS стили
│   └── js/
└── docs/                # Документация
    ├── README.md
    ├── ARCHITECTURE.md      # Этот файл
    └── DEVELOPMENT.md       # Руководство для разработчиков
```

## Принципы архитектуры

### 1. Модульность
- Каждый модуль отвечает за свою функциональность
- Модули независимы друг от друга
- Легко добавлять новые модули

### 2. Разделение ответственности
- **config/**: Настройки и конфигурация
- **modules/**: Бизнес-логика приложения
- **utils/**: Вспомогательные функции
- **static/**: Статические ресурсы

### 3. Простота для начинающих
- Четкая структура папок
- Понятные названия файлов
- Подробная документация
- Комментарии в коде

## Модули приложения

### Базовые модули (modules/base/)
- **data_loader.py**: Загрузка файлов (CSV, HTML)
- **data_parser.py**: Парсинг и структурирование данных
- **visualizer.py**: Отображение данных в Streamlit

### Функциональные модули
- **question_analysis/**: Анализ и визуализация вопросов
- **statistics/**: Общая статистика
- **discrimination/**: Анализ дискриминации
- **reports/**: Генерация отчетов
- **search/**: Поиск и фильтрация
- **settings/**: Настройки приложения

## Как добавить новый модуль

1. Создайте папку в `modules/` с названием модуля
2. Добавьте файлы `__init__.py` и `module.py`
3. В `module.py` создайте функцию `render()`
4. Добавьте модуль в `config/settings.py`
5. Импортируйте и добавьте в `app_new.py`

## Конфигурация

Все настройки хранятся в `config/settings.py`:
- Настройки страницы Streamlit
- Конфигурация модулей
- Поддерживаемые типы файлов
- Пороговые значения для анализа

## Утилиты

В `utils/` находятся вспомогательные функции:
- **helpers.py**: Общие функции (безопасное преобразование типов, определение классов)
- **constants.py**: Константы приложения

## Статические файлы

В `static/` хранятся ресурсы:
- **css/styles.css**: CSS стили для интерфейса
- **js/**: JavaScript файлы (если понадобятся)

## Преимущества архитектуры

1. **Масштабируемость**: Легко добавлять новые функции
2. **Поддерживаемость**: Каждый модуль можно развивать независимо
3. **Понятность**: Четкая структура для начинающих
4. **Тестируемость**: Модули можно тестировать отдельно
5. **Переиспользование**: Базовые модули используются всеми функциональными модулями
