# 🔄 Руководство по миграции

## Переход от старой к новой архитектуре

### Что изменилось

#### Старая структура (app.py)
- Все в одном файле (618 строк)
- Смешанная логика
- Сложно расширять
- Трудно понимать для начинающих

#### Новая структура (app_new.py)
- Модульная архитектура
- Разделение ответственности
- Легко расширять
- Понятно для начинающих

### Как перейти на новую версию

#### 1. Запуск новой версии
```bash
# Вместо
streamlit run app.py

# Используйте
streamlit run app_new.py
```

#### 2. Проверка работоспособности
1. Запустите `app_new.py`
2. Загрузите тот же файл, что использовали в старой версии
3. Проверьте, что все функции работают

#### 3. Если что-то не работает
- Проверьте, что все файлы созданы
- Убедитесь, что импорты работают
- Посмотрите на ошибки в консоли

### Преимущества новой архитектуры

#### Для пользователей
- ✅ Тот же функционал
- ✅ Тот же интерфейс
- ✅ Лучшая производительность

#### Для разработчиков
- ✅ Легче добавлять новые функции
- ✅ Понятная структура кода
- ✅ Возможность тестирования модулей
- ✅ Переиспользование кода

### Структура миграции

#### Что перенесено
- ✅ Загрузка данных (data_loader.py)
- ✅ Парсинг данных (data_parser.py)
- ✅ Визуализация (visualizer.py)
- ✅ Модуль анализа вопросов (question_analysis/)
- ✅ CSS стили (static/css/styles.css)
- ✅ Утилиты (utils/helpers.py)

#### Что добавлено
- ✅ Модульная архитектура
- ✅ Конфигурация (config/settings.py)
- ✅ Документация (docs/)
- ✅ Заготовки для новых модулей
- ✅ Улучшенная структура кода

### Разработка новых модулей

#### Пример: Добавление модуля "Экспорт"
1. Создайте папку `modules/export/`
2. Добавьте файлы:
   ```python
   # modules/export/__init__.py
   # (пустой файл)
   
   # modules/export/module.py
   import streamlit as st
   
   def render():
       st.markdown("### 📤 Модуль экспорта")
       st.write("Экспорт данных в различные форматы")
   ```
3. Добавьте в `config/settings.py`:
   ```python
   MODULES = {
       # ... существующие модули
       "export": {
           "name": "📤 Модуль 7: Экспорт",
           "description": "Экспорт данных в различные форматы"
       }
   }
   ```
4. Обновите `app_new.py`:
   ```python
   from modules.export.module import render as render_export
   
   # В функции main():
   tab_functions = {
       # ... существующие функции
       6: render_export
   }
   ```

### Отладка

#### Если модуль не загружается
```python
# Проверьте импорты в app_new.py
try:
    from modules.my_module.module import render
    print("✅ Модуль загружен")
except ImportError as e:
    print(f"❌ Ошибка импорта: {e}")
```

#### Если CSS не загружается
```python
# Проверьте путь к файлу
import os
css_path = 'static/css/styles.css'
if os.path.exists(css_path):
    print("✅ CSS файл найден")
else:
    print("❌ CSS файл не найден")
```

### Обратная совместимость

#### Старая версия (app.py)
- Остается рабочей
- Можно использовать для справки
- Не рекомендуется для разработки

#### Новая версия (app_new.py)
- Рекомендуется для использования
- Активная разработка
- Лучшая архитектура

### Следующие шаги

1. **Протестируйте новую версию**
   - Запустите `app_new.py`
   - Проверьте все функции
   - Сравните с `app.py`

2. **Изучите новую архитектуру**
   - Прочитайте `docs/ARCHITECTURE.md`
   - Изучите `docs/DEVELOPMENT.md`
   - Посмотрите примеры в модулях

3. **Начните разработку**
   - Выберите модуль для развития
   - Изучите существующий код
   - Добавьте новую функциональность

### Поддержка

Если у вас возникли проблемы с миграцией:
1. Проверьте, что все файлы созданы
2. Убедитесь, что импорты корректны
3. Изучите документацию в `docs/`
4. Сравните с рабочим примером в `modules/question_analysis/`
