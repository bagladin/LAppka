# ⚡ Быстрая настройка Git для GitHub

## 🎯 Цель
Разместить проект LAppka на GitHub для пользователя **bagladin** (bagladin@mail.ru)

## ✅ Что уже готово
- Git настроен: `bagladin <bagladin@mail.ru>`
- Проект готов к размещению
- `.gitignore` настроен правильно

## 🚀 Быстрый старт (с правами администратора)

⚠️ **ВАЖНО:** Если папка `.git` существует, но git выдает ошибку "not a git repository", значит репозиторий поврежден. Сначала удалите его.

Откройте **Терминал** (Terminal.app) и выполните:

```bash
# 1. Перейти в папку проекта
cd "/Users/aleksandrbojko/Documents/LAppka (перед деплоем, копия мак)"

# 2. Удалить поврежденную папку .git (если есть)
# Если возникнет "permission denied", используйте: sudo rm -rf .git
rm -rf .git || sudo rm -rf .git

# 3. Инициализировать git с правами администратора
sudo git init

# 4. Исправить права на файлы (чтобы они принадлежали вам)
sudo chown -R $(whoami) .git

# 5. Переименовать ветку в main
git branch -M main

# 6. Добавить все файлы
git add .

# 7. Создать первый коммит
git commit -m "Initial commit: подготовка проекта к деплою на Streamlit"

# 8. Проверить статус
git status
```

## 📤 Размещение на GitHub

### Шаг 1: Создать репозиторий на GitHub

1. Откройте https://github.com и войдите как **bagladin**
2. Нажмите **"+"** → **"New repository"**
3. Название: `LAppka`
4. Описание: "Учебная аналитика Moodle - приложение для анализа тестовых данных"
5. Выберите **Public** или **Private**
6. **НЕ** ставьте галочки на README, .gitignore, license
7. Нажмите **"Create repository"**

### Шаг 2: Привязать и отправить код

```bash
# Добавить remote репозиторий
git remote add origin https://github.com/bagladin/LAppka.git

# Отправить код на GitHub
git push -u origin main
```

**Если попросит аутентификацию:**
- Username: `bagladin`
- Password: **Personal Access Token** (не пароль!)
  - Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
  - Выберите права: `repo`
  - Скопируйте токен и используйте как пароль

## ✅ Готово!

Проверьте: https://github.com/bagladin/LAppka

## 📚 Подробные инструкции

- `GITHUB_SETUP.md` - полная инструкция
- `GIT_SETUP_FIX.md` - решение проблем
