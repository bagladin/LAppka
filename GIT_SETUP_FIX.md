# 🔧 Решение проблемы с Git в Yandex.Disk

## Проблема
Yandex.Disk блокирует создание файлов в папке `.git`, что мешает инициализации git репозитория.

## Решение: Скопировать проект в обычную папку

### Шаг 1: Скопировать проект

Откройте терминал и выполните:

```bash
# Создать папку для проекта вне Yandex.Disk
mkdir -p ~/Documents/LAppka

# Скопировать весь проект
cp -r "/Users/aleksandrbojko/Documents/Yandex.Disk.localized/Я программист/LAppka (перед деплоем, копия мак)"/* ~/Documents/LAppka/

# Перейти в новую папку
cd ~/Documents/LAppka
```

### Шаг 2: Инициализировать Git

⚠️ **Если возникает ошибка "Operation not permitted", используйте вариант с правами администратора:**

**Вариант А: С правами администратора (РЕКОМЕНДУЕТСЯ)**

Откройте **Терминал** (не Cursor) и выполните:

```bash
# Перейти в папку проекта
cd "/Users/aleksandrbojko/Documents/LAppka (перед деплоем, копия мак)"

# Инициализировать git с правами администратора
sudo git init

# Исправить права на файлы (чтобы они принадлежали вам, а не root)
sudo chown -R $(whoami) .git

# Переименовать ветку в main
git branch -M main

# Проверить статус
git status
```

**Вариант Б: Обычная инициализация (если права уже настроены)**

```bash
# Инициализировать git
git init

# Переименовать ветку в main
git branch -M main

# Проверить статус
git status
```

### Шаг 3: Добавить файлы и сделать первый коммит

```bash
# Добавить все файлы
git add .

# Сделать первый коммит
git commit -m "Initial commit: подготовка проекта к деплою"
```

### Шаг 4: Создать репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите **"+"** → **"New repository"**
3. Название: `LAppka`
4. Описание: "Учебная аналитика Moodle - приложение для анализа тестовых данных"
5. Выберите **Public** или **Private**
6. **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
7. Нажмите **"Create repository"**

### Шаг 5: Привязать к GitHub

```bash
# Добавить remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/LAppka.git

# Отправить код на GitHub
git push -u origin main
```

**Если попросит аутентификацию:**
- Используйте **Personal Access Token** (не пароль)
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- Выберите права: `repo` (полный доступ к репозиториям)
- Используйте токен как пароль при `git push`

## Альтернативный вариант: Временно отключить синхронизацию

Если хотите работать прямо в Yandex.Disk:

1. Откройте настройки Yandex.Disk
2. Временно отключите синхронизацию для этой папки
3. Выполните `git init`
4. Включите синхронизацию обратно

⚠️ **Внимание:** Yandex.Disk может конфликтовать с git, поэтому лучше работать в обычной папке.

## После настройки

После успешной настройки git в новой папке:
- Работайте с проектом из `~/Documents/LAppka`
- Yandex.Disk папку можно использовать как резервную копию
- Все изменения делайте в обычной папке и коммитьте в git
