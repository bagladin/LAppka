# 🚀 Инструкция по настройке GitHub

## Шаг 1: Настройка Git (если еще не настроено)

Git уже настроен для пользователя **bagladin** (bagladin@mail.ru). Проверить можно командой:

```bash
git config --global --list
```

Если нужно изменить настройки:

```bash
git config --global user.name "bagladin"
git config --global user.email "bagladin@mail.ru"
```

## Шаг 2: Инициализация Git репозитория

⚠️ **ВАЖНО:** Если при выполнении `git init` возникает ошибка "Operation not permitted", используйте один из вариантов ниже.

### Вариант А: С правами администратора (РЕКОМЕНДУЕТСЯ)

Откройте **Терминал** (не Cursor) и выполните:

```bash
# Перейти в папку проекта (текущее расположение)
cd "/Users/aleksandrbojko/Documents/LAppka (перед деплоем, копия мак)"

# Или если проект все еще в Yandex.Disk:
# cd "/Users/aleksandrbojko/Documents/Yandex.Disk.localized/Я программист/LAppka (перед деплоем, копия мак)"

# Инициализировать git с правами администратора
sudo git init

# Исправить права на файлы (чтобы они принадлежали вам, а не root)
sudo chown -R $(whoami) .git

# Переименовать ветку в main
git branch -M main
```

### Вариант Б: Разрешить доступ в настройках macOS

1. Откройте **Системные настройки** → **Конфиденциальность и безопасность** → **Полный доступ к диску**
2. Убедитесь, что **Терминал** (Terminal.app) добавлен в список разрешенных приложений
3. Если терминала нет, нажмите **"+"** и добавьте его
4. Перезапустите терминал
5. Выполните:

```bash
cd "/Users/aleksandrbojko/Documents/LAppka (перед деплоем, копия мак)"
git init
git branch -M main
```

**Примечание:** Если проект находится в Yandex.Disk, используйте путь:
```bash
cd "/Users/aleksandrbojko/Documents/Yandex.Disk.localized/Я программист/LAppka (перед деплоем, копия мак)"
```

### Вариант В: Создать репозиторий через веб-интерфейс GitHub

Если оба варианта не работают, создайте репозиторий на GitHub через веб-интерфейс (см. Шаг 5), затем клонируйте его:

```bash
cd ~/Documents
git clone https://github.com/bagladin/LAppka.git
# Затем скопируйте файлы проекта в клонированную папку
```

## Шаг 3: Проверка статуса репозитория

```bash
# Текущее расположение проекта
cd "/Users/aleksandrbojko/Documents/LAppka (перед деплоем, копия мак)"

# Или если проект в Yandex.Disk:
# cd "/Users/aleksandrbojko/Documents/Yandex.Disk.localized/Я программист/LAppka (перед деплоем, копия мак)"

git status
```

## Шаг 4: Добавление файлов в репозиторий

```bash
# Добавить все файлы (кроме тех, что в .gitignore)
git add .

# Проверить, что будет добавлено
git status
```

## Шаг 5: Первый коммит

```bash
git commit -m "Initial commit: подготовка проекта к деплою"
```

## Шаг 6: Создание репозитория на GitHub

1. Откройте https://github.com в браузере
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
4. Заполните форму:
   - **Repository name**: `LAppka` (или другое имя)
   - **Description**: "Учебная аналитика Moodle - приложение для анализа тестовых данных"
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть эти файлы)
5. Нажмите **"Create repository"**

## Шаг 7: Привязка локального репозитория к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды:

```bash
# Добавить удаленный репозиторий
git remote add origin https://github.com/bagladin/LAppka.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код на GitHub
git push -u origin main
```

Если GitHub попросит аутентификацию:
- Используйте **Personal Access Token** (не пароль)
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- Выберите права: `repo` (полный доступ к репозиториям)
- Скопируйте токен и используйте его как пароль при `git push`

## Шаг 8: Проверка

Откройте ваш репозиторий на GitHub - все файлы должны быть там!

## 📝 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть изменения
git diff

# Добавить конкретный файл
git add имя_файла.py

# Создать коммит
git commit -m "Описание изменений"

# Отправить изменения на GitHub
git push

# Получить изменения с GitHub
git pull

# Посмотреть историю коммитов
git log --oneline
```

## ⚠️ Важные замечания

1. **Никогда не коммитьте**:
   - `.venv/` (виртуальное окружение)
   - `__pycache__/` (кэш Python)
   - `.env` (секретные ключи)
   - Личные тестовые файлы

2. Все эти файлы уже добавлены в `.gitignore`

3. После каждого изменения кода:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```

## 🔧 Установка зависимостей

Если виртуальное окружение еще не настроено или нужно переустановить зависимости:

```bash
# Активация виртуального окружения
source .venv/bin/activate

# Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt
```

## 🚀 Запуск приложения

```bash
# Просто запустите скрипт
./start.sh

# Или вручную
source .venv/bin/activate
streamlit run app_new.py
```
